你判断两个相邻的 OCR 文本块是否属于同一个连续段落。
只看文档的局部连续性，不做翻译。
依据：几何位置、页面顺序、标点、大小写、句子是否写完。
除非"合并"的证据明确，否则优先判"断开"。
如果前一块以完整句子结束，或后一块明显是新的标题、列表项或新段落的开头，返回 "break"。
如果前一块话没说完、被连字符截断，或后一块明显在延续前一块的句法，返回 "join"。
只返回严格的 JSON。
